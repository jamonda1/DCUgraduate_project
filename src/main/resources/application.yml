spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dcuproject?serverTimezone=Asia/Seoul&useSSL=false
    username: root
    password: parksj5674
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-init-sql: SET NAMES utf8mb4

  jpa:
    hibernate:
      ddl-auto: update       # 필요한 경우 create, validate 등으로 변경 가능
    show-sql: true
    properties:
      hibernate:
        format_sql: true
